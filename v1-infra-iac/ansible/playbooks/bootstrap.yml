- name: Bootstrap machines
  hosts: all
  become: true

  tasks:
    - name: Ensure apt cache is up to date
      apt:
        update_cache: true

    - name: Install curl
      apt:
        name: curl
        state: present