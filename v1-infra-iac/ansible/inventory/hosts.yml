all:
  vars:
    ansible_python_interpreter: /usr/bin/python3

  children:
    k3s_cluster:
      children:

        k3s_control_plane:
          hosts:
            cp:
              ansible_host: 192.168.1.20
              ansible_user: younes
              k8s_node_name: k3s-cp #useful for tainting cp

        k3s_workers:
          hosts:
            worker1:
              ansible_host: 192.168.1.21
              ansible_user: younes
            worker2:
              ansible_host: 192.168.1.12
              ansible_user: younes
            worker3:
              ansible_host: 192.168.1.13
              ansible_user: younes
          

